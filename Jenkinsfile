pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'building python code'
            }
        }
        stage('run') {
            steps {
                echo 'running the container'
            }
        }
    }
}
