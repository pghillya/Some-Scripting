pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo 'building python code'
            }
        }
        stage('run') {
            steps {
                echo 'running the container'
            }
        }
    }
}
# Adding a comment
